#%Module1.0
proc ModulesHelp { } {
    puts stderr "This module sets up environment for clas12-workflow"
}
module-whatis   "clas12-workflow 0.8"

set d /group/clas12/packages/clas12-workflow/0.8

module load rcdb
module load root
#prereq rcdb
#prereq root

prepend-path PYTHONPATH $d/lib/util
prepend-path PYTHONPATH $d/lib/swif
prepend-path PYTHONPATH $d/lib/clas12

setenv CLAS12WFLOW $d

prepend-path PATH $d/scripts

#if { [module-info mode load] } {
#    puts stderr ">>>"
#    puts stderr ">>> WARNING:  clas12-workflow models have changed:"
#    puts stderr ">>>           - model names are now strings" 
#    puts stderr ">>>           - negative phaseSize is the default and uses no phases, only dependencies"
#    puts stderr ">>>           - without --workDir, phased decode+merge tasks are combined into one job"
#    puts stderr ">>>"
#    puts stderr ">>>           See:" 
#    puts stderr ">>>           https://github.com/baltzell/clas12-workflow/blob/master/README.md"
#    puts stderr ">>>"
#}

